// Converts graph JSON to IR (Intermediate Representation)
// This is a stub. Extend logic for your graph structure.

/**
 * @param {Object} graph - The graph JSON from frontend
 * @returns {Object} IR - Intermediate Representation
 */
export default function buildIR(graph) {
  // Example: just wrap graph in IR object
  // Replace with real graph-to-IR logic
  return { nodes: graph.nodes || [], edges: graph.edges || [] };
}
